# Copy to keystore.properties (gitignored) on the machine that builds RELEASE APKs.
# Without this file, builds fall back to debug signing (fine for development/testing).
storeFile=C:\\path\\to\\smsgateway.keystore
storePassword=CHANGE_ME
keyAlias=smsgateway
keyPassword=CHANGE_ME
