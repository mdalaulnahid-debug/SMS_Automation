<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Dark mode: full re-declaration since Android resource configs replace, not merge,
         identically-named styles. Keep in sync with values/themes.xml — only the two
         windowLight*Bar flags differ. -->
    <style name="Theme.SmsGateway" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="colorPrimary">@color/accent</item>
        <item name="colorPrimaryVariant">@color/accent_muted</item>
        <item name="colorOnPrimary">@color/accent_on</item>
        <item name="colorSecondary">@color/accent</item>
        <item name="colorOnSecondary">@color/accent_on</item>
        <item name="android:windowBackground">@color/bg_primary</item>
        <item name="android:statusBarColor">@color/bg_primary</item>
        <item name="android:navigationBarColor">@color/bg_primary</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
        <item name="materialButtonStyle">@style/Widget.SmsGateway.Button</item>
        <item name="textInputStyle">@style/Widget.SmsGateway.TextInputLayout</item>
        <item name="tabStyle">@style/Widget.SmsGateway.TabLayout</item>
        <item name="bottomNavigationStyle">@style/Widget.SmsGateway.BottomNav</item>
    </style>
</resources>
